.layout.layout-stack-sm.layout-main-left
  .col-sm-7.col-md-8.layout-main
    #viewHoursContainer
  .col-sm-5.col-md-4.layout-sidebar
    #addHoursContainer

%script
  var activities = $.parseJSON("#{j @activities.to_json.html_safe}")
  var hours = $.parseJSON("#{j @volunteer_hours.to_json.html_safe}")
  var user = $.parseJSON("#{j current_user.to_json.html_safe}").id
  DashboardApp.initialize(activities, hours, user);

