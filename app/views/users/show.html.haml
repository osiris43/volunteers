.layout.layout-stack-sm.layout-main-left
  .col-sm-7.col-md-8.layout-main
    %h1 Users#show
    %p Find me in app/views/users/show.html.haml
  .col-sm-5.col-md-4.layout-sidebar
    #addHoursContainer

%script
  var activities = $.parseJSON("#{j @activities.to_json.html_safe}")
  var current_user = $.parseJSON("#{j current_user.to_json.html_safe}")
  console.log(current_user)
  DashboardApp.initialize(activities);

